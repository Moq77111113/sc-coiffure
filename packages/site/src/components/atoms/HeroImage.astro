---
import { Image } from 'astro:assets';
import hero from '~/assets/hero.jpg';

const sizes =
  '(max-width: 480px) 100vw,(max-width: 768px) 90vw, 1200px';
---

<Image
  class="pointer-events-none h-full w-full object-cover"
  src={hero}
  sizes={sizes}
  alt="Notre salon"
/>
