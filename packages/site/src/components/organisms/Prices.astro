---
import ContentSection from '~/components/molecules/ContentSection.astro';
import { categories } from '~/constants/prices';
import Price from '~/components/atoms/Price.svelte';
const [_categoryName, prices] = categories.introPrices;
---

<ContentSection id="prices" classes="bg-offset px-2 sm:px-12">
  <Fragment slot="title">
    <h2 class=`text-center font-extrabold tracking-tight text-6xl text-offset`>
     Nos tarifs
    </h2>
  </Fragment>
  <div class="text-xs text-center px-8 text-offset">
  <p >Chez SC Coiffure,  nous créons des coupes sur mesure selon vos préférences de longueur. </p>
   <p class="mt-2">
    Optez pour le court, le mi-long, ou le long, et découvrez la coupe parfaite qui reflète votre style unique dans nos tarifs</p>
  </div>
  <ul class="grid max-w-6xl grid-cols-1 sm:grid-cols-2">
    {prices.map((price, i) => 
    <li
      class=`flex flex-col items-center gap-4 p-6 ${prices.length % 2 !== 0 && i === prices.length -1 ? "sm:col-span-2 sm:mx-auto" : ""}`
    >
    <Price client:load {price} />
    </li>)}
  </ul>

  <span class="text-xs text-center text-offset">
    Contactez-nous pour un devis personnalisé et plus de tarifs !
</span>

  </ContentSection>
